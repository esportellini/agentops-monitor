import { cn } from "@/lib/utils";
const green=new Set(["ok","success","healthy","approved","priced","active","completed","passed"]);const amber=new Set(["degraded","warning","warn","pending","pending_approval","medium","unpriced","open"]);const red=new Set(["error","failed","blocked","rejected","critical","high"]);const blue=new Set(["running","info","processing"]);
export function StatusBadge({status,className}:{status:string;className?:string}){const value=status.toLowerCase();const tone=green.has(value)?"status-green":amber.has(value)?"status-amber":red.has(value)?"status-red":blue.has(value)?"status-blue":"status-neutral";return <span className={cn("status-badge",tone,className)}><span aria-hidden="true"/>{status.replaceAll("_"," ")}</span>}
